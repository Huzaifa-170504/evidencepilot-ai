# Tool package boundary

EvidencePilot's direct arXiv, Crossref, Tavily, document-search, and model provider adapters are isolated in `apps/api/app/services`. The read-only standardized variants live in `packages/mcp_server`.

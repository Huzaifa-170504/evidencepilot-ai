# Shared contracts

Pydantic API contracts are defined in `apps/api/app/schemas.py`; generated Supabase TypeScript contracts are committed at `apps/web/src/types/database.ts`. OpenAPI remains the source of truth between the frontend and backend.
